// function declaration
/*to declare a function we use
function keyword which is function
then give it a name claire followed by parenthesis
where we can pass in our arguments(parameters), then curly braces
so in between the curly braces is the lexical environment where 
we write our code
*/

function add(a, b) {
    return a + b
}
console.log(add(15, 5));