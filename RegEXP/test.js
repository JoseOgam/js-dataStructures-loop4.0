/*
regular expression is an object, can be constructed using RegExp constructor or using literals and enclosing characters
inside a forward slash
*/

let rel1 = new RegExp("abcd");

let rel2 = /abcd/;
console.log(rel2);
